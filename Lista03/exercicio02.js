const total = 10000;
const popular = total * 0.10;
const geral = total * 0.50;
const arquibancada = total * 0.30;
const cadeiras = total * 0.10;
const rendaTotal = popular * 5 + geral * 10 + arquibancada * 20 + cadeiras * 40;
console.log(rendaTotal);